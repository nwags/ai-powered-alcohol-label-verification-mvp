# Fixtures Directory

Optional OCR smoke-test fixture:

- `sample_label.jpg` used by `GET /api/v1/demo/sample/sample_label`

Add real image files locally for demos. This repo keeps only placeholder structure.
